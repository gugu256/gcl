#include <stdio.h>
#include "gcl/color.h"
#include "gcl/file.h"
#include "gcl/str.h"

int main() {
    /*int lenofccomp = file_length("/Users/evatzug/Desktop/lightfair.json");
    char readrez[lenofccomp];
    printf("The file is %d characters long\n", lenofccomp);
    read_file(readrez, "/Users/evatzug/Desktop/lightfair.json");
    printf("%s\n", readrez);

    char mytext[] = "if this works, i may buy champagne\nthough i'm not sure if my bank account is ready for it.";
    write_file(mytext, "test.txt");

    char firsthingy[] = "hahahiha";
    char secondthingy[] = "hohahiha";

    if (is_equal(firsthingy, secondthingy)) {
        printf("It's the same string!\n");
    } else {
        printf("It's not the same string\n");
    }

    char firsthingy2[] = "hahahiha";
    char secondthingy2[] = "hahahi";

    if (is_equal(firsthingy2, secondthingy2)) {
        printf("It's the same string!\n");
    } else {
        printf("It's not the same string\n");
    }

    char firsthingy3[] = "hahahiha";
    char secondthingy3[] = "hahahiha";

    if (is_equal(firsthingy3, secondthingy3)) {
        printf("It's the same string!\n");
    } else {
        printf("It's not the same string\n");
    }*/

    //printf("Hello world\n");
    //printf("%s%s%s%s%sYou can also get crazy with it%s%s%s!!%s\n", BACK_GREEN, FORE_RED, STYLE_UNDERLINE, STYLE_BOLD, STYLE_BLINK, RESET_ALL, BACK_BLUE, FORE_YELLOW, RESET_ALL);
    // char ctest1[] = "this is the first test";
    // test_firstchar(ctest1);
    // char* ctest2 = "and this is the second test";
    // test_firstchar(ctest2);
    // if (str_starts_with("hoyhoy", "hoihoihei")) {printf("ef\n");}
    // if (str_starts_with("hoehoe", "hoehoehei")) {printf("fe\n");}
    return 0;
}